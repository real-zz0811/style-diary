# ─────────────────────────────────────────────────────────────────────
# 环境变量模板
#
# 复制本文件为 .env.local 后填入自己的值：
#   Windows PowerShell:  Copy-Item .env.example .env.local
#   macOS / Linux:       cp .env.example .env.local
#
# .env.local 已被 .gitignore 排除（*.local），不会进入版本库。
# 取值位置：Supabase 控制台 → Project Settings → API
# 详细图文步骤见 README-SETUP.md 第三节。
# ─────────────────────────────────────────────────────────────────────

# 项目地址，形如 https://xxxxxxxx.supabase.co（别忘了 https://）
VITE_SUPABASE_URL=

# 可公开的客户端密钥，下面两个变量名**二选一**填写即可：
#   · 新控制台叫 publishable key，以 sb_publishable_ 开头 → 填 VITE_SUPABASE_PUBLISHABLE_KEY
#   · 老控制台叫 anon public key，以 eyJ 开头              → 填 VITE_SUPABASE_ANON_KEY
#
# 说明：这个 key 本来就会出现在前端构建产物里，它不是"不能见人的秘密"——
# 真正拦住越权读写的是数据库的行级安全策略（RLS，见 supabase/schema.sql）。
# 但 service_role key 权限等同于管理员，**绝对不能**填在这里。
VITE_SUPABASE_ANON_KEY=

# 只填 publishable key 时，把这行的 # 去掉，并把上面那行留空
# VITE_SUPABASE_PUBLISHABLE_KEY=
